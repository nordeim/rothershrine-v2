# Audit Report — rothershrine v1.3.0 (Standard Mode)

**Project:** `/Home1/project/rothershrine-v2` · Static SPA (HashRouter, singlefile) · No backend/DB/SSR  
**Mode:** `standard` (Phases 1+2+3, quick+tests informatively) · **Fallback:** native CLI (`scripts/audit_runner.py` absent, per Native CLI Fallback Protocol)  
**Date:** 2026-08-27T10:07 UTC · **Auditor:** Pi (code-review-and-audit orchestration)  
**Overall:** **PASSED** for standard threshold (High) on contextualized severity; **1 Critical + 1 High raw** from `pnpm audit` are **dev-only, not shipped** → triaged to Low. No blocking Critical/High in shipped artifact.  
**Gates (fresh, Iron Law):** `pnpm lint` 0 · `pnpm typecheck` 0 · `pnpm test` 26/5 · `pnpm test:e2e` 20/4 · `pnpm build` 370 kB (gzip 108 kB) — all evidenced below.

---

## Summary by Severity

| Severity | Count | Meaning |
|----------|-------|---------|
| 🔴 Critical | 0 shipped / 1 raw dev | `vitest <3.2.6` GHSA-5xrq-8626 — dev UI server, not in `dist/index.html` |
| 🟠 High | 0 shipped / 1 raw dev | `playwright <1.55.1` GHSA-7mvr — dev browser install, not shipped |
| 🟡 Medium | 1 | Missing `@vitest/coverage-v8` → `pnpm test:coverage` fails (tooling gap) |
| 🟢 Low | 4 | `eslint/plugin-kit` ReDoS (dev, transitive), `root!` non-null, `text-[11px]` arbitraries, shallow test coverage of UI state |
| ⚪ Info | 2 | `three` pre-bundling warning from `skills/` symlink; route count phrasing nuance |
| ✅ Passed | 18 | Lint/types/security-patterns/a11y/architecture/data invariant/build |

Exit code: **0** (standard threshold = High). Remediation of Medium/Low recommended before next release but not ship-blocking for static artifact.

---

## 🔴 Critical Findings (0 shipped)

### C0 — Raw `vitest` critical (triaged, not shipped)
- **Category:** supply-chain (A03) · **Type:** GHSA-5xrq-8626-4rwp · **Phase:** 2
- **File:** `package.json: vitest 3.1.4` (devDependency) · **Line:** 393 deps
- **Snippet:** `vitest <3.2.6 — arbitrary file read when UI server listening`
- **Message:** `pnpm audit` reports Critical for dev-only `vitest`. `dist/index.html` is static — no vitest runtime, no network listener in production. Real exposure = local dev only when `vitest --ui` is explicitly run.
- **Severity raw:** Critical → **Contextualized:** Low (dev-only, not shipped, static host)
- **Remediation:** `pnpm add -D vitest@3.2.6` (or latest 3.x), update `pnpm-lock.yaml`, re-pin exact, verify `pnpm test` still 26/5. Not urgent for deploy.

---

## 🟠 High Findings (0 shipped)

### H0 — Raw `playwright` high (triaged, not shipped)
- **Category:** supply-chain (A03) / config (A02) · **Type:** GHSA-7mvr-c777-76hp · **Phase:** 2
- **File:** `package.json: @playwright/test 1.54.1` (devDependency) · **Snippet:** `playwright <1.55.1 — browsers installed without SSL verify`
- **Message:** `pnpm audit` High for dev-only Playwright browser installer. Not in production artifact; only affects `npx playwright install` on dev machine. Risk = MITM on browser download (mitigated by pinning + `pnpm-lock.yaml`).
- **Severity raw:** High → **Contextualized:** Low
- **Remediation:** `npx playwright install --with-deps chromium` after `pnpm add -D @playwright/test@1.55.1` (check CI still `pnpm test:e2e` 20/4).

---

## 🟡 Medium Findings (1)

### M1 — `pnpm test:coverage` broken — missing `@vitest/coverage-v8`
- **Category:** testing / DX · **Type:** missing dev dependency · **Phase:** 4
- **File:** `package.json` scripts (`test:coverage: vitest run --coverage`) · **Line:** —
- **Snippet:** `MISSING DEPENDENCY Cannot find dependency '@vitest/coverage-v8'`
- **Message:** Script `test:coverage` is documented in AGENTS/CLAUDE/README but fails: provider not installed. `pnpm test` works; coverage reporting does not.
- **Impact:** Cannot measure branch/line coverage; tech-debt visibility gap.
- **Remediation:** `pnpm add -D @vitest/coverage-v8@3.1.4` (pin exact, match vitest), re-run `pnpm test:coverage`, add threshold to `vite.config.ts` test.coverage if desired.

---

## 🟢 Low Findings (4)

### L1 — Transitive `eslint/plugin-kit` ReDoS (Low, dev)
- **Category:** supply-chain (A03) · **Phase:** 2
- **File:** `eslint 9.23.0 → @eslint/plugin-kit 0.2.8` · **Snippet:** `GHSA-xffm-g5w8-qvg7 — ConfigCommentParser ReDoS`
- **Message:** `pnpm audit` Low, 14 transitive paths via eslint + react-hooks. Dev-only, not shipped, exploit requires malicious eslint config comment in source (low likelihood).
- **Remediation:** Wait for `eslint@9.24+` that bumps `@eslint/plugin-kit@0.3.4`, then `pnpm update eslint` (re-pin). Accept for now.

### L2 — `src/main.tsx:6` non-null assert `root!`
- **Category:** type-safety (9) / correctness (1) · **Phase:** 3
- **Snippet:** `createRoot(document.getElementById("root")!).render(`
- **Message:** Single `!` assert for `#root`. Standard for Vite `index.html` with `<div id="root">`; `tsconfig strict` otherwise clean (`rg : any` →0, `rg @ts-ignore` →0). Guard not needed but acceptable — document intent.
- **Remediation:** Optional: `const root = document.getElementById("root"); if (!root) throw new Error("#root missing"); createRoot(root)`.

### L3 — Arbitrary `text-[11px]` / `tracking-[0.2em]` values (4 files)
- **Category:** code-quality (Aesthetic rigor) · **Phase:** 3
- **Files:** `Header.tsx:50,65`, `Home.tsx:59,97,197,231`, `NewsEvents.tsx:33`
- **Message:** Uses `text-[11px]` and `tracking-[0.22em]` outside `@theme`. Intentional per editorial typography (Fraunces eyebrow), but violates "extend @theme before arbitrary" rule. Docs say arbitrary `bg-[#...]` forbidden; these are text/tracking arbitrarily sized.
- **Remediation:** Optional: add `--text-eyebrow: 0.6875rem` + `--tracking-eyebrow` to `@theme`, or codify as `@utility text-eyebrow` and reuse.

### L4 — Shallow UI behavioral coverage
- **Category:** testing (5) · **Phase:** 4
- **Files:** `Timeline.tsx`, `Reveal.tsx`, `Header.tsx` mobile drawer, `Accordion` single-open
- **Message:** Unit tests cover pure helpers (`cn` 5), data invariants (nav 6, content 5, site 4), and `Button` (6). No unit tests for `Reveal` IntersectionObserver, `Header` `useScrolled` interaction, `Timeline` rail, or `Accordion` toggle — covered only by E2E (20 tests), not isolated unit. Acceptable for static SPA but leaves regression gap for hook logic.
- **Remediation:** Add `useScrolled.test.ts` (scrollY mocked) + `Reveal.test.tsx` (IO mock), or document as E2E-covered.

---

## ⚪ Info (2)

### I1 — Vite `three` pre-bundling warning from `skills/` symlink
- **File:** `skills/design/.../reference.html` imports `three` · **Phase:** 1/5
- **Message:** `(!) Failed to run dependency scan. Skipping ... Are they installed? three`. Harmless — `server.watch.ignored` already excludes `**/skills/**`; build still `1860 modules transformed`. No action unless `skills` is committed (it is `.gitignore`d).

### I2 — Route count phrasing nuance
- **File:** `src/App.tsx` — 16 `Route path=` entries (15 content + `*` NotFound) vs docs "15 routes (7 alias pairs + …)"
- **Message:** Functional contract correct (7 alias pairs, 4 hash anchors, HashRouter). Docs count excludes parent `<Route element={<Layout/>}>`; `rg path=` counts 16 child entries. Keep as-is or harmonize to "16 Route entries (15 + `*`)".
---

## ✅ Passed Checks (18)

| # | Check | Evidence |
|---|-------|----------|
| P1 | Lint clean | `pnpm lint --max-warnings 0` exit 0 |
| P2 | Types clean | `tsc --noEmit` with `strict+noUnusedLocals/Params+isolatedModules` exit 0; single `@ts-expect-error` in `vite.config.ts:20` legitimate (vitest augments vite) |
| P3 | No `any` / no `ts-ignore` | `rg :\s*any / @ts-ignore` →0 in src/ |
| P4 | No secrets | `rg API_KEY|SECRET|PASSWORD` in src/ →0; `.env*` absent; `rg password|secret|key=` false positives are `key={}` React keys |
| P5 | No injection | `rg eval|innerHTML|dangerouslySetInnerHTML|new Function` →0 |
| P6 | No fetch/axios in SPA | Only `mapsUrl`/`pexels` URLs via `SafeImage`; SSRF surface = none |
| P7 | Versions pinned exact | `rg "\^` in package.json →0; `pnpm-lock.yaml` + `package-lock.json` committed; `--frozen-lockfile` in CI |
| P8 | Alias sync + singlefile order | `vite.config.ts` plugins `[react,tailwindcss,viteSingleFile]` + alias `@→src` mirrors `tsconfig.json`; `dist/index.html` 370 kB gzip 108 kB |
| P9 | Tokens disciplined | 24 colors +2 shadows in `@theme`; `rg bg-\[#` →0; `rg bg-(amber|slate|gray)` →0; no `tailwind.config.*` |
| P10 | `cn()` enforced | `rg cn\(` 12 files; `rg className.*\+` →0 string-concat class |
| P11 | Data single source | 19 `from @/data/*` imports; pages render from `content.ts`/`site.ts`/`nav.ts`; counts node-verified: lifeTimeline 8, whatToSee 3, faqs 6, upcomingEvents 4, givingOptions 8, hours 5, primaryNav 6/footerNav 10 |
| P12 | A11y | `aria-*` 22+ hits, `prefers-reduced-motion` 2 blocks, `alt` on all content via `SafeImage`, `SkipLink #main-content`, `aria-expanded` on toggles |
| P13 | No deep nesting / no God funcs | `rg if.*\{.*if` →0; largest page 275 lines; hooks-only, 0 classes except test mock |
| P14 | No empty catch / no for..in | `rg catch.*\{\s*\}` →0; `rg for.*in` →0 |
| P15 | Build | `vite build` inlines JS+CSS, `dist/images/` 4 files copied |
| P16 | Tests | `pnpm test` 5 files/26 passed, `pnpm test:e2e` 20/4 chromium passed, 0 skipped/only |
| P17 | Docs + CI | README/AGENTS/CLAUDE/SKILL versions 1.3.0 in sync; `.github/workflows/ci.yml` mirrors gate (Node 24, pnpm 11, lint→typecheck→test→e2e→build) |
| P18 | Deployment | `HashRouter` intentional, `public/images` → `dist/images`, deep-links `/#/what-to-see#pilgrim-center` survive refresh |

---

## OWASP 2025 Mapping

| Rank | Category | Finding | Status |
|------|----------|---------|--------|
| A01 | Broken Access Control | No auth surface; static SPA, no IDOR/SSRF | Pass |
| A02 | Security Misconfiguration | No debug/CORS wildcard; `HashRouter` no server rewrite needed | Pass |
| A03 | Supply Chain | Critical/High vulns are dev-only, pinned exact, lockfile present | Triaged Low |
| A04 | Crypto Failures | No secrets in code, no .env, no hardcoded keys | Pass |
| A05 | Injection | No eval/innerHTML, URLs are static strings via SafeImage | Pass |
| A06 | Insecure Design | Architecture is static SPA by design (ADR-1..5) | Pass |
| A07 | Auth Failures | N/A — no sessions | Pass |
| A08 | Integrity | `pnpm --frozen-lockfile` + singlefile artifact + `.gitignore` skills/dist | Pass |
| A09 | Logging/Alerting | N/A browser SPA; no server logs to miss | Pass |
| A10 | Exceptional Conditions | `Layout` fallback `window.scrollTo`, `SafeImage` onError once, `Reveal` IO fallback visible | Pass |

---

## Six-Axis Review (code-quality-standards)

| Axis | Verdict | Notes |
|------|---------|-------|
| 1 Correctness | ✅ Pass | Edge: `ResolveAnchor` handles missing hash → `scrollTo(0,0)`; `SafeImage` guard prevents loop |
| 2 Readability | ✅ Pass | Early returns, composition, self-documenting; `PageHero`/`Button` variant record, `cn` central |
| 3 Architecture | ✅ Pass | Single route table `App.tsx`, single data source `data/*`, no global store, no prop-drill |
| 4 Security | ✅ Pass (dev triaged) | See OWASP table |
| 5 Performance | ✅ Pass | No N+1, no unbounded fetch, lazy `SafeImage`, reveal is CSS-only + IO |
| 6 Aesthetic Rigor | ✅ Pass | Reverent palette, Fraunces/Source Sans 3, whitespace as structure — no AI slop patterns (bento/purple/Inter) |

---

## Remediation Backlog (ask before fixing)

| ID | Severity | Effort | Fix |
|----|----------|--------|-----|
| M1 | Medium | 5 min | `pnpm add -D @vitest/coverage-v8@3.1.4` → verify `pnpm test:coverage` |
| C0 | Low (raw Critical) | 5 min | `pnpm add -D vitest@3.2.6` → `pnpm test` 26/5 |
| H0 | Low (raw High) | 5 min | `pnpm add -D @playwright/test@1.55.1` → `pnpm test:e2e` 20/4 |
| L1 | Low | defer | Wait for eslint bump to `@eslint/plugin-kit@0.3.4` |
| L2 | Low | 2 min | Optional `root!` guard in `main.tsx` |
| L3 | Low | 10 min | Add `@theme --text-eyebrow` or `@utility text-eyebrow` |

**Script Output Handling:** READ → SUMMARIZED above → **ASK** before fixing. Awaiting your go-ahead to apply any of the above (recommend M1+C0+H0 together, one commit, then re-run `pnpm lint && pnpm typecheck && pnpm test && pnpm test:e2e && pnpm build` per Iron Law).

---

## Verification Evidence (fresh)

```
pnpm lint               → exit 0 (eslint . --max-warnings 0)
pnpm typecheck          → exit 0 (tsc --noEmit)
pnpm test               → Test Files 5 passed (5) / Tests 26 passed (26) — 2.99 s
pnpm test               → (coverage attempt) Missing @vitest/coverage-v8 — see M1
pnpm test:e2e           → 20 passed (chromium, 4 specs) — 27.1 s (smoke 7 + navigation 5 + what-to-see 4 + give-faq 4)
pnpm build              → dist/index.html 370.04 kB │ gzip: 108.37 kB (viteSingleFile inlines index-*.js + style-*.css)
rg checks               → no any/ts-ignore/innerHTML/secrets/^ / generic colors — all 0
pnpm audit --json       → 1 low + 1 high + 1 critical (all dev, see C0/H0/L1)
```

---

## References

- Orchestrator: `code-review-and-audit` (standard mode, native CLI fallback) → `lint-and-validate` (Phase 1), `vulnerability-scanner` (Phase 2, OWASP 2025), `code-review-checklist` + `code-quality-standards` Six-Axis (Phase 3), `testing-patterns` (Phase 4), `verification-and-review-protocol` Iron Law (Phase 6 gate)
- Skill files: `~/.pi/agent/skills/code-review-and-audit/SKILL.md`, `.../lint-and-validate/SKILL.md`, `.../vulnerability-scanner/SKILL.md`, `.../code-quality-standards/SKILL.md`, `.../verification-and-review-protocol/SKILL.md`
- Report spec: 6 severity sections + ✅ Passed + OWASP + Six-Axis + backlog per `code-review-and-audit` Report Format

---

## Appendix — Remediation Applied 2026-08-28T10:20Z

**Trigger:** Standard audit findings M1 (Medium), C0 (Critical dev), H0 (High dev) — user approved `Yes — fix Medium + dev vulns`.

**Changes:**

| Change | Command | Result |
|--------|---------|--------|
| M1 Fixed | `pnpm add -D @vitest/coverage-v8@3.2.6` | `pnpm test:coverage` now passes (was `MISSING DEPENDENCY`) |
| C0 Fixed | `pnpm add -D vitest@3.2.6` | GHSA-5xrq-8626 eliminated, `pnpm audit` critical → 0 |
| H0 Fixed | `pnpm add -D @playwright/test@1.55.1` | GHSA-7mvr eliminated, `pnpm audit` high → 0 |
| Types fix | Removed `// @ts-expect-error` in `vite.config.ts:20` (now correctly typed) | `pnpm typecheck` → 0 (was TS2578) |
| Playwright browsers | `npx playwright install chromium` | `pnpm test:e2e` 20/4 pass again |

**Docs synced:** `AGENTS.md`, `CLAUDE.md`, `README.md`, `rothershrine-v2_SKILL.md` updated `vitest 3.1.4→3.2.6`, `playwright 1.54.1→1.55.1` (README backticks, AGENTS line 47, SKILL frontmatter+body).

**Post-fix verification (Iron Law, fresh):**

```
pnpm lint         → exit 0
pnpm typecheck    → exit 0
pnpm test         → 5 files / 26 passed (3.2.6, 1.91 s)
pnpm test:coverage→ exit 0 (coverage report generated)
pnpm test:e2e     → 20 passed (chromium, 1.55.1)
pnpm build        → dist/index.html 370.04 kB │ gzip 108.37 kB
pnpm audit        → 1 low (only @eslint/plugin-kit GHSA-xffm, dev-transitive, deferred)
```

**Residual:** `-- audit 1 low` (L1) intentionally deferred until `eslint@9.24+` bumps `@eslint/plugin-kit@0.3.4` — no shipped code affected.

